import java.util.ArrayList;

public class Geometry {
    public static void main(String[] args) {
        // Her er en ArrayListe, som kan holde på objekter af typen Shape.
        ArrayList<Shape> shapes = new ArrayList<>();

        // Lav nu forskellige Circle og Rectangle objekter, og tilføj dem til den nyoprettede ArrayList.


        for (Shape shape : shapes) {
            // Lav en variabel der hedder area og som holder på værdien, som returneres af shape.calcuculateArea();

            // For hver shape i listen
            // brug instanceof til at tjekke hvilken type den pågældende Shape i listen er.
            // Afhængig af hvilken type shape viser sig at være, så udskrive noget tekst om, at der er tale om en udregning af et den konkrete (sub)types areal. Inddrag her også værdien af area variablen.

        }
    }


}
